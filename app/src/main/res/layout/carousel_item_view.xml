<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.jackandphantom.carouselrecyclerview.view.ReflectionImageView
        android:id="@+id/reflection_image_view"
        android:layout_width="115dp"
        android:layout_height="140dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/character_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/white"
        tools:text="name"
        tools:textColor="@color/black"
        app:layout_constraintTop_toBottomOf="@id/reflection_image_view"
        app:layout_constraintStart_toStartOf="@id/reflection_image_view"
        app:layout_constraintEnd_toEndOf="@id/reflection_image_view"
        />

</androidx.constraintlayout.widget.ConstraintLayout>